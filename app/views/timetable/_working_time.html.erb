Working time
<%= turbo_frame_tag 'work_time_form' do %>
  <%= link_to "#{@slots_by_day_hours.keys.first.strftime('%H:%M')} - #{(@slots_by_day_hours.keys.last + 30.minutes ).strftime('%H:%M') }",
              new_working_time_timetable_index_path(selected_day: @selected_day),
              data: { turbo_frame: "work_time_form" } %>
<% end %>