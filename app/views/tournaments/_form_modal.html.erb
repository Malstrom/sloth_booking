<div class="modal fade" id="tournamentModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Create new tournament</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <%= form_with(model: Tournament.new, url: club_tournaments_path(@club)) do |form| %>
        <div class="modal-body d-flex">
          <%= hidden_field_tag :selected_day, @selected_day %>

          <%= form.hidden_field(:day, value: @selected_day) %>

          <div class="mb-3">
            <%= form.label :rating, class: "col-form-label" %>
            <%= form.text_field :rating, class: "form-control w-50" %>
          </div>

          <div class="mb-3">
            <%= form.label :price, class: "col-form-label" %>
            <%= form.number_field :price, class: "form-control w-50" %>
          </div>
        </div>
        <div class="modal-footer">
          <button type="submit" class="btn btn-primary">Save tournament</button>
        </div>
      <% end %>
    </div>
  </div>
</div>
