<div class="modal fade" id="<%= dom_id(tournament) %>" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Редактирование турнира</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <%= form_with model: [@club, tournament] do |form| %>
        <div class="modal-body">
          <%= hidden_field_tag :selected_day, @selected_day %>

          <%= form.hidden_field(:day, value: @selected_day) %>

          <div class="mb-3">
            <%= form.label :rating, class: "col-form-label" %>
            <%= form.text_field :rating, class: "form-control" %>
          </div>
          <div class="mb-3">
            <%= form.label :price, class: "col-form-label" %>
            <%= form.text_field :price, class: "form-control" %>
          </div>
        </div>
        <div class="modal-footer">
          <%= button_to "Удалить", club_tournament_path(@club, tournament, selected_day: @selected_day), id:"tournament_#{tournament.id}_delete", class:"btn link-danger btn-link", method: :delete %>
          <button type="submit" class="btn btn-primary btn-sm">Сохранить</button>
      <% end %>

      <button type="button" class="btn btn-outline-primary float-end btn-sm" data-bs-dismiss="modal"
              value="<%= {bookable_id: tournament.id, bookable_type: "Tournament"}.to_json %>"
              data-controller="hello" data-action="click->hello#selectKind">
        Выбрать ячейки
      </button>
      </div>
    </div>
  </div>
</div>
